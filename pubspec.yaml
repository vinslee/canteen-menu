name: canteen_menu
description: 学校食堂菜谱管理应用
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  path_provider: ^2.1.2
  screenshot: ^2.1.0
  image_gallery_saver: ^2.0.3
  permission_handler: ^11.3.0
  intl: ^0.19.0
  share_plus: ^7.2.2

flutter:
  uses-material-design: true
